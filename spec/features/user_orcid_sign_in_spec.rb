require 'spec_helper'

describe "sign in via ORCID", type: :feature, js: true do
  # before(:each) { sign_in }

  # it 'should be signed in' do
  #   visit '/'
  #   expect(page).to have_css "h1", "API Requests"
  # end
end
