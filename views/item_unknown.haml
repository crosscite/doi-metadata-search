%tr
  %td.item-data
    - unless item.title.nil?
      %p.lead
        = item.title
        
      %p.extra
        %span
          - if item.version
            = "Version " + item.version + ". "
          Published
          %b
            - unless item.day.nil?
              = item.day
            - unless item.month.nil?
              = item.month
            - unless item.year.nil?
              = item.year              
        - unless item.publication.nil?
          %span
            via
            %b= item.publication
  
    - unless item.authors.nil?
      %p.expand= item.authors[0..19].join(", ")
      
    - if item.creative_commons
      %p
        - if item.creative_commons == "zero"
          %a{:href => "http://creativecommons.org/publicdomain/zero/1.0/"}
            %img{:src => "http://i.creativecommons.org/p/zero/1.0/80x15.png"}
        - else
          %a{:href => "http://creativecommons.org/licenses/#{item.creative_commons}/3.0/"}
            %img{:src => "http://i.creativecommons.org/l/#{item.creative_commons}/3.0/80x15.png"}

    = partial :item_links, :item => item
        
