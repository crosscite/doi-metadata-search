%div.item-data
  %h3
    - if item.title
      = item.title
    - else
      [no metadata registered]

  - unless item.authors.nil?
    %p= item.authors[0..19].join(", ")

  %p
    - if item.version
      = "Version " + item.version + " of "
    - if item.subtype.present?
      = item.subtype
      published
    - elsif item.type
      = item.type
      published
    - else
      Work published
    - unless (item.year.nil? || item.year.to_i == 0)
      - unless item.day.nil?
        = item.day
      - unless item.month.nil?
        = item.month
      - unless (item.year.nil? || item.year.to_i == 0)
        = item.year
    - unless item.publication.nil?
      via
      = item.publication

  = haml :item_links, locals: { item: item }
