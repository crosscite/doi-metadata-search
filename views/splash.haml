:javascript
  $(document).ready(function() {
    $('#help-body').collapse('hide');
    $('#search-input').focus();
  });

.row-centered.break-below
  = partial :search_header, :page => page.merge({:mini_header => true})
  
.spacer  
.row-centered
  %h1
    %img{:src => '/images/cms-logo.png'}
  %form{:method => 'GET', :action => '/'}
    .search
      %input.input.input-xlarge.search-input#search-input{:name => 'q', :type => 'text', :value => page[:query]}
      %i.icon-search

.row-centered
  .accordion#help-accordion
    .accordion-group
      .accordion-heading
        %a.accordion-toggle{:href => '#help-body', 'data-toggle' => 'collapse', 'data-parent' => '#help-accordion'}
          .pull-right
            %i.icon-caret-down
          %span Help! - Example Queries
      .accordion-body#help-body  
        .accordion-inner
          = partial :help_list, :page => page

