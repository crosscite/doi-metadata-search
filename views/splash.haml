= haml :"#{ENV['RA']}/header", locals: { params: {} }

.container-fluid
  - unless flash[:error].nil?
    .alert.alert-error
      = flash[:error]

  .centered
    %img{class: 'logo-big', src: "//#{ENV['CDN_HOST']}/assets/logo-big.png"}

  .row
    .col-md-6.col-md-offset-3
      = haml :'works/search'

    .splash

= haml :"#{ENV['RA']}/footer"
